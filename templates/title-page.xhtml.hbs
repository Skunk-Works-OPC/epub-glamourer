<?xml version='1.0' encoding='UTF-8'?>
<!DOCTYPE html>
<html xmlns="http://www.w3.org/1999/xhtml"
      xmlns:epub="http://www.idpf.org/2007/ops"
      xml:lang="{{language}}">
<head>
  <meta charset="utf-8"/>
  <title>{{title}}</title>
  <link href="{{cssPath}}" rel="stylesheet" type="text/css"/>
</head>
<body>
  <section epub:type="titlepage" class="title-page-contain">
    <div class="title-page">
      <span class="ornament">❧</span>
      <p class="book-title">{{title}}</p>
      {{#if subtitle}}<p class="book-subtitle">{{subtitle}}</p>{{/if}}
      <p class="book-author">{{author}}</p>
      {{#if publisher}}<p class="book-publisher">{{publisher}}</p>{{/if}}
      {{#if date}}<p class="book-publisher">{{date}}</p>{{/if}}
    </div>
    <div class="ec-brand titlepage">
      <img src="images/estorya-classics.svg" alt="eStorya Classics" class="ec-logo logo-cover"/>
      <p class="ec-tagline">Modern editions of timeless stories.</p>
    </div>
  </section>
</body>
</html>
